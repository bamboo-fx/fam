{
  "buildCommand": "cd backend && bun install && bunx prisma generate",
  "devCommand": "cd backend && bun run dev",
  "installCommand": "cd backend && bun install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index.ts"
    }
  ]
}
